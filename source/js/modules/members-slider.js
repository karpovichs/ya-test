import Slider from './slider'

export default () => {
    const membersSlider = new Slider({
        'main': '.js-slider-members',
        'spaceBetween': 20,
        'mobileSpaceBetween': 0,
    })
}
